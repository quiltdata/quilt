description "uWSGI server instance configured for quilt-server"

start on runlevel [2345]
stop on runlevel [!2345]

setuid www-data
setgid www-data

env PATH=/var/www/quilt/bin
chdir /var/www/quilt
exec uwsgi --ini quilt_server.ini
