query ($id: ID!) {
  dataProduct(id: $id) {
    id
    name
    createdAt
    ownerRole {
      __typename
      ... on ManagedRole {
        name
      }
      ... on UnmanagedRole {
        name
      }
    }
    members {
      objects {
        logicalKey
        bucket
        key
        versionId
      }
      packages {
        virtualName
        bucket
        name
        hashOrTag
      }
    }
  }
}
