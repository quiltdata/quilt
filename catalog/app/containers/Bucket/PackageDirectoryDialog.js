import * as React from 'react'
import * as M from '@material-ui/core'

export default function PackageDirectoryDialog({ open }) {
  return (
    <M.Dialog fullWidth open={open} scroll="body">
      <h1>It works</h1>
    </M.Dialog>
  )
}
