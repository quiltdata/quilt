// import { fromJS } from 'immutable';
// import { makeSelectProfileDomain } from '../selectors';

// const selector = makeSelectProfileDomain();

describe('makeSelectProfileDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
