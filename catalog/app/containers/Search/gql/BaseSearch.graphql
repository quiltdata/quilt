query ($searchString: String, $filter: SearchFilterExpression) {
  search(searchString: $searchString, filter: $filter) {
    __typename
    ... on BoundedSearch {
      results {
        total
      }
      facets {
        source
        name
        path
        type
        extents
      }
    }
    # ... on UnboundedSearch { }
    ... on InvalidInput {
      errors {
        path
        message
        name
        context
      }
    }
    ... on OperationError {
      message
      name
      context
    }
  }
}
