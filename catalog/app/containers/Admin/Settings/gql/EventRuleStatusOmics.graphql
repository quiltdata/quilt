query {
    admin {
        eventRuleStatus(ruleType: OMICS) {
            ... on EventRuleStatusSuccess {
                ruleArn
                enabled
            }
            ... on OperationError {
                name
                message
                context
            }
        }
    }
}
