# import PolicyResultSelection from "./PolicyResultSelection.graphql"

mutation ($input: UnmanagedPolicyInput!) {
  policyCreate: policyCreateUnmanaged(input: $input) {
    ...PolicyResultSelection
  }
}
