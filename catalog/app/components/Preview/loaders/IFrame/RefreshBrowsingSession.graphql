mutation ($id: ID!, $ttl: Int) {
  browsingSessionRefresh(id: $id, ttl: $ttl) {
    __typename
    ... on BrowsingSession {
      id
      expires
    }
    ... on OperationError {
      message
    }
  }
}
