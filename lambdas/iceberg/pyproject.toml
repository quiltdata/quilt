[project]
name = "t4_lambda_iceberg"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Sergey Fedoseev", email = "fedoseev.sergey@quiltdata.io" }
]
requires-python = "==3.11.*"
dependencies = [
    "boto3>=1.40.41,<2.0.0",
    "quilt-shared[boto]",
]

[build-system]
requires = ["uv_build>=0.8.22,<0.9.0"]
build-backend = "uv_build"

[dependency-groups]
test = [
    "pytest>=8.4.2",
    "pytest-cov>=7.0.0",
    "pytest-env>=1.1.5",
    "pytest-mock>=3.15.1",
]

[tool.uv.sources]
quilt-shared = { url = "https://github.com/quiltdata/quilt/archive/7ba1b6e82c81f5e2d4c1b90f11afb72f76ee994d.zip", subdirectory = "py-shared" }
